# AirMark 개선 계획 — 두 계획의 상세 비교와 통합안

## 1. 검토 결론

[다른 구현 계획]( /Users/baemingwan/Documents/AI/airmark/airmark_editor_plan.md )은 **TextKit 2의 구체적인 구현 경로와 초기 검증 항목**에서 강점이 있다. 특히 문단 단위 스타일링, 편집 범위 밖의 문단 무효화, TextKit 1 전환 감지, 무작위 편집 테스트는 적극 채택한다.

반면 **핵심 UX를 후순위로 미룬 점, 자체 파서의 호환성, 레이아웃과 WebKit 동작을 검증된 사실처럼 단정한 부분**은 수정해야 한다. 내 기존 계획도 표시 좌표와 무효화 절차가 충분히 구체적이지 않았으므로 함께 보완한다.

이번 개선안은 이전 계획을 대체한다. 파일은 검토만 했으며 아직 수정하지 않았다.

### 받아들일 점

| 항목 | 평가와 반영 |
|---|---|
| 순수 원문 저장소 + 문단별 표시 속성 | 채택. 저장·undo와 시각적 스타일을 분리하는 중심 구조로 삼는다. |
| 레이아웃 속성과 색상 속성 분리 | 채택. 글꼴·간격 변화와 색상 변화에 다른 무효화 경로를 사용한다. |
| 편집 범위 밖 문단의 재생성 검증 | 채택. 펜스·목록·참조 링크 변경 테스트에 포함한다. |
| TextKit 1 전환 감지 | 채택. 호환성 모드 전환을 자동 테스트 실패로 처리한다. |
| 증분 처리와 전체 처리의 결과 비교 | 채택. 파서뿐 아니라 위치 인덱스·캐시·스타일 결과에도 적용한다. |
| Foundation 코어와 AppKit 계층 분리 | 채택. UI 없이 핵심 로직을 빠르게 테스트한다. |
| 원본 이미지 다운샘플링과 렌더 캐시 | 채택. 스크롤 중 이미지 디코딩을 피한다. |
| 명령행 빌드·실행·성능 스크립트 | 채택. 동일한 절차로 반복 검증할 수 있게 한다. |
| 상태 복원과 마지막 문서 복원 구분 | 채택. 마지막 활성 문서를 별도로 기록한다. |
| 변경 근거와 실측 결과 기록 | 채택. 설계 결정과 재현 조건을 개발 로그에 남긴다. |

**환경 조사도 정정한다.** 다운로드 폴더에 Xcode 27 베타가 있으며, `DEVELOPER_DIR`를 지정한 `xcodebuild -version`이 정상 실행됐다. 새 Xcode 설치는 필요하지 않다. 다만 macOS 26 실행 검증 환경은 별도로 확보해야 한다.

## 2. 상세 검토: 수정해야 할 약점

### ① 핵심 편집 경험이 첫 버전에서 빠져 있다 — 최우선 수정

다른 계획은 마커를 항상 표시하는 편집기를 먼저 만들고, 커서 위치에 따른 기호 숨김과 인라인 수식을 Phase 3으로 미룬다.

사용자가 확정한 경험은 **현재 편집 부분만 원문으로 보이는 일체형 편집**이다. 이 동작은 뒤에 얹는 장식이 아니다. 커서 좌표, 선택, 줄바꿈, 수식 크기, undo 구조에 영향을 준다.

**개선:** 첫 실행 가능한 검증판부터 기호 접기·인라인 수식·한글 조합 입력을 함께 구현한다. 이 조합이 성립하기 전에는 일반 기능을 확장하지 않는다.

### ② 자체 증분 파서는 속도보다 호환성 위험이 먼저다

라인 인덱스와 변경 범위 추적은 유용하지만, 제안된 파서는 들여쓰기 코드, 참조 링크 해석, 목록의 이어지는 문단 등을 축소한다. CJK 강조 규칙도 별도로 바꾼다. 결과적으로 같은 파일이 GitHub나 다른 편집기와 다르게 보일 수 있다.

또한 다음 두 테스트는 의미가 다르다.

- **증분 결과 = 전체 스캔 결과:** 자체 구현 내부의 일관성을 검사한다.
- **결과 = Markdown 명세:** 문법 해석의 정확성을 검사한다.

둘 다 같은 오류를 내면 첫 번째 테스트는 통과한다. CommonMark는 여러 줄에 걸친 인라인 문법과 문서 전역 참조도 정의하므로 단순한 라인 상태만으로 처리하기 어렵다. [CommonMark 명세](https://spec.commonmark.org/0.31.2/)

**개선:** 의미 해석은 `swift-markdown`에 맡긴다. 자체 구현은 위치 인덱스, 편집 중인 문법 마커의 위치, 수식 확장 구간, 변경 추적에 한정한다. 전체 재파싱 비용이 실제 병목으로 확인되기 전에는 새 Markdown 파서를 만들지 않는다.

### ③ 문법상의 줄과 TextKit 문단을 같다고 보면 안 된다

계획은 `LF`, `CRLF`, U+2028, U+2029를 같은 줄 구분자 집합으로 다룬다. 하지만 CommonMark가 정의하는 줄바꿈은 LF·CR·CRLF다. TextKit의 표시 문단 경계와 Markdown의 문법 경계는 별도로 취급해야 한다. [줄 정의](https://spec.commonmark.org/0.31.2/#characters-and-lines)

또한 파일 전체를 LF로 정규화한 뒤 원래 줄바꿈 “종류 하나”만 기억하면, `CRLF`와 `LF`가 섞인 파일을 그대로 복원할 수 없다.

**개선:** 원문은 정규화하지 않는다. Markdown 줄 인덱스와 TextKit 문단 범위를 별도로 유지하고 교차 범위를 계산한다. 파싱용 변환이 필요하면 변환본과 원문 사이의 위치 대응을 유지한다.

### ④ 렌더링 공간 확보가 편집 동작까지 해결해 주지는 않는다

`bottomMargin`으로 그림을 넣을 공간을 확보하는 접근은 검증할 가치가 있다. 그러나 원문 아래에 이미지를 그리는 것만으로 다음이 해결되지는 않는다.

- 원문을 접은 자리에 결과를 표시하는 일체형 전환
- 이미지·수식 클릭 시 정확한 원문 진입
- 선택 하이라이트와 키보드 탐색
- 인라인 수식의 기준선·높이·줄바꿈
- 스크린리더가 인식할 의미

극소 글꼴과 음수 자간 역시 검증할 구현 수단이지, 커서와 선택이 자연히 맞는다는 근거는 아니다. 복잡한 수식을 기존 한 줄 높이에 맞춰 축소하면 가독성이 떨어진다.

표도 탭 정지점만 설정해서는 원문의 `|` 구분자가 정렬되지 않는다. 실제 셀 너비 계산과 표 표시 계층이 필요하다.

**개선:** 모든 시각 요소에 원문 범위·표시 영역·클릭 동작·접근성 정보를 연결한다. 인라인 수식은 실제 ascent/descent에 맞춰 줄 높이를 확보한다. 화면에 그려지는 모양과 편집 동작을 각각 테스트한다.

### ⑤ macOS 26에서 사용할 수 없는 대안 API가 포함됐다

로컬 SDK를 대조한 결과:

- 일반적인 viewport delegate 자체는 이전 버전에도 존재한다.
- `NSTextViewportRenderingSurface`는 **macOS 27 API**다.
- `NSTextView`에서 해당 viewport 콜백을 공식적으로 override하는 진입점도 SDK상 **macOS 27부터**다.

따라서 다른 계획의 “macOS 26 렌더링 서피스 대안”을 그대로 채택할 수 없다.

**개선:** 기본 편집 경로는 macOS 26에서 공개된 API로 구성한다. macOS 27 전용 API는 초기 구현의 필수 의존성에서 제외한다. 베타 SDK에서 빌드되는 것과 최소 지원 OS에서 실행되는 것을 별도로 검증한다.

### ⑥ 숨겨진 WebView의 측정과 촬영 단계가 불완전하다

1×1 WebView 안에서 DOM을 화면 밖에 배치한 채 스냅샷을 얻는 설계는 그대로 사용할 수 없다. Apple SDK는 스냅샷 사각형이 WebView의 경계 안에 있어야 한다고 명시한다. DOM 크기를 측정하는 것과 그 영역을 이미지로 촬영하는 것은 별도 작업이다. [WKSnapshotConfiguration](https://developer.apple.com/documentation/webkit/wksnapshotconfiguration)

또한 “창 밖 WebView는 언제나 렌더링 불가”, “지연 생성하면 시작 비용이 0”이라는 단정도 피해야 한다. 첫 문서에 다이어그램이 있으면 렌더러 생성이 초기 작업과 경쟁할 수 있다.

**개선:** 촬영 전에 WebView와 DOM을 실제 출력 크기로 배치하고, 촬영 영역이 bounds 안에 들어오도록 한다. 가려진 창·최소화·다른 Space·창 종료 상황을 검증한다. 시작 화면이 입력 가능해진 뒤 렌더 작업을 시작하며, 최소화 중에는 작업을 보류한다.

### ⑦ SwiftMath 우선·KaTeX 폴백은 비용과 일관성을 함께 검증해야 한다

SwiftMath의 네이티브 렌더링은 유망하다. 다만 `<1ms`라는 수치는 이번 환경에서 측정되지 않았고, “파싱 성공”만으로 KaTeX와 같은 시각 결과가 보장되지 않는다. 수식마다 엔진이 바뀌면 글꼴·간격·기준선·줄바꿈이 달라질 수 있다.

동시성 설명도 수정이 필요하다. 확인한 `MTFontManager`에는 `@RWLocked` 보호가 있으므로 “싱글턴이 있으니 반드시 데이터 레이스”라고 단정할 수 없다. 반대로 `@preconcurrency import`가 동시성 안전성을 만들어 주는 것도 아니다. [SwiftMath 구현](https://github.com/mgriebling/SwiftMath/blob/main/Sources/SwiftMath/MathRender/MTFontManager.swift), [Swift 동시성 마이그레이션](https://www.swift.org/migration/documentation/swift-6-concurrency-migration-guide/incrementaladoption/)

**개선:** 첫 버전은 KaTeX 하나로 수식 결과를 통일한다. SwiftMath는 이후 별도 최적화 대상으로 남기며, 도입하려면 수식 코퍼스의 호환성·시각 품질·실제 메모리 절감부터 입증한다.

### ⑧ 캐시·파일 저장·성능 측정의 계약이 부족하다

추가로 보완해야 할 부분은 다음과 같다.

| 문제 | 수정 |
|---|---|
| 캐시 키가 원문·테마·배율뿐 | 너비, 글꼴, 수식 표시 모드, 렌더러 버전도 포함 |
| 렌더 결과가 `CGImage`뿐 | 논리 크기, 기준선, 접근성 설명과 결과 식별 정보 추가 |
| 오래된 결과의 적용 조건 누락 | 문서·블록·수정 번호를 확인하고 적용 |
| 이미지 붙여넣기를 “모두 undo 가능”으로 처리 | 파일 생성·Save As·미저장 문서 자산까지 별도 설계 필요. 첫 버전은 기존 로컬 이미지 표시로 제한 |
| NSDocument가 복원을 모두 제공한다고 표현 | 초안 복구, 마지막 활성 문서, 충돌·권한 상실은 앱에서 보완 |
| 키 입력부터 표시까지 8ms | 120Hz의 프레임 간격과 전체 입력 지연을 구분 |
| 창이 key가 된 시점으로 첫 프레임 추정 | 창 활성화·본문 배치·표시·입력 가능 시점을 분리 |
| 10회 평균 중심 측정 | 반복 실행의 p50·p95, 첫 실행과 OS 캐시 상태를 구분 |
| XCTest 전면 금지 | 단위 테스트는 Swift Testing, 실제 UI 자동화에는 XCTest/XCUIAutomation 사용 |

Apple도 Swift Testing은 단위·직접 호출 통합 테스트에, XCTest는 UI·성능 테스트에 사용하는 구성을 안내한다. 다른 프로젝트의 관례를 이 프로젝트의 절대 제약으로 가져오지 않는다. [Apple 테스트 구성](https://developer.apple.com/documentation/xcode/adding-tests-to-your-xcode-project)

## 3. 개선된 구현 설계

### 제품과 범위

- 이름은 **AirMark**, macOS 26 이상·Apple Silicon 전용으로 한다.
- 실행하면 마지막 활성 문서와 선택·스크롤 위치를 복원한다. 첫 실행에는 자동 보관되는 빈 노트를 연다.
- Finder에서 파일을 열면 그 문서를 우선하고 불필요한 빈 창을 만들지 않는다.
- 기본 편집 모드는 **현재 편집 부분만 원문 표시**다. 기호를 항상 표시하는 모드는 보조 옵션으로 제공한다.
- 첫 버전에는 CommonMark/GFM 편집, 표·체크리스트·코드·로컬 이미지, 검색·바꾸기, Mermaid, 인라인·블록 수식, 자동 저장·복원을 포함한다.
- 폴더 관리·탭·내보내기·원격 이미지 다운로드·자산 가져오기·자체 동기화는 후속으로 둔다.

### 프로젝트와 빌드

- **얇은 Xcode 앱 타깃 + 로컬 SwiftPM 모듈**로 구성한다. `.app` 패키징과 UI 테스트는 Xcode가 담당한다.
- 코어 모듈은 `swift test`로 검사한다. 빌드·실행·벤치마크 스크립트는 같은 설정을 사용한다.
- 앱·편집 UI는 MainActor, 파서·작업 스케줄러는 명시적인 별도 격리를 적용한다.
- 파서·편집기·렌더러·문서 저장을 분리하고 UI 없는 코어에는 AppKit을 넣지 않는다.
- 의존성 버전과 리소스를 고정하고 라이선스를 포함한다.
- 초기 전달은 비샌드박스 개인용 `.app`과 ad-hoc 서명으로 한다. Developer ID 서명과 공증은 배포 단계에서 추가한다.

### 원문·파싱·표시의 흐름

`원문 편집 → 위치 인덱스 갱신 → 백그라운드 해석 → 변경된 표시 범위 계산 → 해당 문단 갱신`

- 원문 저장소는 하나다. AST를 다시 직렬화해 파일을 저장하지 않는다.
- `SourceIndex`는 UTF-8·UTF-16 위치와 Markdown 줄·TextKit 문단의 대응을 관리한다.
- `swift-markdown`이 Markdown 의미를 결정한다. 별도 문법 위치 추적기는 마커·수식 확장 구간을 보조한다.
- 일반 입력은 즉시 반영하고 파싱 요청은 짧게 모은다. 실행 중인 작업 뒤에는 최신 요청 하나만 유지한다.
- 파싱 결과는 해당 수정 번호에만 적용한다. 현재 상태와 다르면 폐기한다.
- 문단 델리게이트는 준비된 표시 정보를 읽기만 한다. 여기서 파싱·파일 읽기·수식 렌더링을 하지 않는다.

### 문단 무효화와 편집 정확성

- 글꼴·간격·기호 접기 변화는 문단 표시 요소와 레이아웃을 갱신한다.
- 색상만 변하면 rendering attributes를 갱신한다. 이 속성은 재배치 때 무효화될 수 있으므로 재적용 경로도 둔다.
- 편집 범위 밖에서 의미가 달라진 문단은 별도로 재생성한다. 변경 알림은 현재 저장소 콜백 안에서 재진입하지 않도록 모아서 처리한다.
- 한글 조합 중에도 원문과 위치 추적은 유지한다. 조합 범위의 표시 전환만 유예하고, 조합이 끝나면 최신 결과를 적용한다.
- 숨은 마커를 지나는 이동·선택·삭제는 원문 범위 기준으로 처리한다.
- 서식 명령과 체크박스 토글은 정상 편집 경로와 동일한 undo manager를 사용한다. 스타일링 자체는 undo와 저장 상태에 영향을 주지 않는다.
- TextKit 1 전환을 감지하고, 편집 경로에서 `layoutManager` 접근과 전체 문서 강제 레이아웃을 막는다.

### 특수 콘텐츠의 표시

- 마커 접기는 표시용 속성과 공개 TextKit 2 확장으로 처리한다. 원문과 UTF-16 위치를 보존한다.
- 블록은 하나의 논리 요소로 관리한다. 표시 영역을 클릭하면 해당 원문으로 진입한다.
- 인라인 수식은 실제 너비·ascent·descent에 맞춰 공간을 확보한다. 줄보다 넓으면 가로 스크롤 가능한 별도 줄로 표시한다.
- 표는 비활성 상태에서 셀을 측정해 정렬하고, 편집 시 같은 위치에서 원문을 보여준다.
- 그림과 수식이 완성되면 읽던 위치를 기준으로 스크롤을 보정한다. 입력 중 전환에는 애니메이션을 적용하지 않는다.
- 이미지 영역의 클릭을 인접 텍스트에 우연히 맡기지 않고 명시적으로 처리한다.

### 렌더링 서비스

- KaTeX와 Mermaid를 각각 지연 생성하는 최대 두 작업자로 분리한다.
- 번들 리소스만 사용하며 외부 요청을 차단한다. DOM 촬영은 실제 렌더 크기의 WebView에서 수행한다.
- 수식마다 독립 매크로 환경을 사용한다. 초기에는 KaTeX 지원 수학 문법으로 범위를 고정한다.
- `RenderRequest`는 콘텐츠와 렌더 환경을, `RenderArtifact`는 이미지·논리 크기·기준선·접근성 정보를 가진다.
- 캐시 재사용은 콘텐츠와 환경으로 판단하고, 화면 적용은 문서·블록·수정 번호로 판단한다.
- 화면에 보이는 요청을 우선하고 요청 수·픽셀 수·캐시 용량에 상한을 둔다.
- 오류·시간 초과·WebContent 종료는 해당 요소만 원문으로 되돌린다. 본문 입력과 저장은 계속 동작한다.

### 문서와 디자인

- 일반 `.md` 파일, BOM·혼합 줄바꿈·후행 개행을 보존한다.
- 저장 작업은 수정 번호가 붙은 불변 스냅샷을 사용한다. 백그라운드 저장이 편집 중인 저장소를 직접 읽지 않는다.
- 자동 저장과 초안 복구를 구분하고, 실패한 저장을 완료로 표시하지 않는다.
- 외부 수정과 로컬 수정이 충돌하면 양쪽 내용을 보존한다.
- UI는 최소한의 제목 막대, 최대 약 720pt 읽기 폭, 기본 16pt 시스템 글꼴, 약 1.5배 줄 간격으로 시작한다.
- 라이트·다크, 키보드 탐색, 맞춤법 검사, VoiceOver를 기본 검증 대상에 포함한다.

## 4. 검증과 성능 기준

수치는 **미측정 성능 예산**으로 유지한다. 기준은 M1 MacBook Air 8GB·macOS 26의 Release 빌드이며 macOS 27도 별도로 검사한다.

| 항목 | 합격 목표 |
|---|---|
| 100KB 문서로 프로세스 시작 → 본문 표시·입력 가능 | p95 500ms 이내 |
| 실행 중인 앱에서 100KB 문서 열기 | p95 200ms 이내 |
| 일반 입력의 메인 스레드 처리 | p95 4ms 이내 |
| 60Hz 화면에서 입력 → 표시 | p95 33ms 이내 |
| 입력 종료 후 일반 서식 반영 | p95 100ms 이내 |
| 스크롤 | 60Hz에서 안정적 배치, 120Hz는 별도 프레임 예산 검증 |
| 안정 상태 메모리 | 일반 문서 100MB, 특수 렌더 관련 프로세스 합계 300MB 이내 목표 |
| 유휴 CPU | 평균 1% 미만 |

100KB는 빠른 반응의 기준, 1MB는 전체 기능 검사, 10MB는 무정지 입력·메모리·원문 보존 검사로 구분한다. 파일 크기 하나만으로 자동으로 모든 서식을 끄지 않는다.

필수 검증 묶음:

1. **문법·위치:** CommonMark/GFM 예제, 참조 정의 변경, 여러 줄 인라인 문법, 혼합 줄바꿈, U+2028/U+2029, 한글·이모지.
2. **편집:** 한글 조합, 숨은 마커 경계, 수식 원문 진입, 다중 블록 선택, 찾기·바꾸기, 맞춤법 교정, undo/redo.
3. **차등 테스트:** 무작위 편집 후 위치 인덱스와 최종 표시 결과를 새로 계산한 결과와 비교.
4. **렌더링:** 기준선·선명도·창 너비·배율·테마 변경, 오래된 결과, 최소화·창 종료, 오프라인, 오류·시간 초과.
5. **문서:** 저장 실패, 외부 수정, 파일 이동·삭제, 초안 복구, 강제 종료 후 복구 기록 확인.
6. **접근성:** 그림의 원문·설명 접근, 헤딩·링크·표 탐색, 키보드만으로 열기부터 저장까지 수행.

단위·직접 호출 통합 테스트는 Swift Testing, 실제 앱 흐름은 XCTest/XCUIAutomation, 성능은 signpost와 Instruments로 검사한다. 실제 한글 입력기 조합은 별도 수동 검증도 유지한다.

## 5. 실행 순서와 완료 조건

### A. 가장 어려운 편집 경험부터 검증

문단 스타일링, 기호 접기, 인라인 수식, Mermaid, 한글 조합, 선택·undo를 한 편집기에 통합한다.

**통과 조건:** macOS 26 공개 API만으로 입력·선택·줄바꿈·기준선이 정확하며 TextKit 1 전환이 없어야 한다. `bottomMargin`, 마커 축소, 인라인 공간 예약은 이 단계에서 실제로 검증한다. 실패하면 기능 확대를 멈추고 해당 설계를 수정한다.

### B. 문서 신뢰성 완성

파일 열기·저장·복원·초안·외부 변경·저장 실패 처리를 완성한다.

**통과 조건:** 저장·복구 기록에 반영된 내용을 정확히 복원하고, 충돌한 파일을 자동 덮어쓰지 않는다.

### C. 일상 편집 기능 완성

표·체크리스트·코드·로컬 이미지·검색·단축키와 특수 렌더링 캐시를 연결한다.

**통과 조건:** 지원 문법과 편집 동작의 차등·회귀 테스트를 통과하고, 긴 렌더링 중에도 본문 입력이 지연되지 않는다.

### D. 디자인과 성능 마감

타이포그래피, 여백, 선택 표시, 스크롤 보정, 접근성을 다듬고 기준 문서로 성능을 측정한다.

**최종 산출물:** 실행 가능한 `.app`, 재현 가능한 빌드·테스트 절차, 성능 측정 결과, 지원 문법과 알려진 제한, 설계 결정 기록.

**이번 개선의 핵심은 다른 계획의 구체적인 구현·검증 방법을 받아들이면서, 일체형 편집 경험과 원문 정확성을 첫 단계부터 입증하도록 순서를 바꾼 것이다.**
