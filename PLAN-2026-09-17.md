# AirMark 후속 계획 — 2026-09-17

기준 커밋은 `7c58cc1`이다. 이 문서는 [REVIEW.md](REVIEW.md)가 남긴 잔여 위험 여섯 가지와, 이번 계획 수립 과정에서 코드를 다시 읽으며 확인한 문제를 하나의 실행 순서로 묶는다. [PLAN.md](PLAN.md)의 설계 원칙(원문 보존, TextKit 2 전용, 문단 델리게이트의 읽기 전용성, 결과 적용 조건의 명시)은 그대로 유지한다. 이 문서는 계획이며, 아래에 적힌 수치 중 "합격 조건"은 아직 측정되지 않은 목표다.

## 1. 품질 기준을 관찰 가능한 문장으로 바꾼다

목표로 삼은 OpenMark와 Lettera는 소스가 공개되지 않은 상용 앱이다. 두 앱의 코드를 참고할 수는 없다. 대신 그 앱들이 사용자에게 약속하는 성질을 우리가 검증할 수 있는 문장으로 옮긴다. Lettera는 편집 대상이 데이터베이스가 아니라 파일 그 자체라는 점과 "거의 보이지 않는 작은 것들"의 마감을 내세운다. OpenMark는 네이티브, 오프라인, 키보드 우선, 그리고 Mermaid·KaTeX·표·체크리스트가 기본 동작한다는 점을 내세운다. 이것을 기준으로 삼으면 다음 여덟 항목이 된다.

| 기준 | 관찰 가능한 조건 | 현재 상태 | 담당 작업 |
|---|---|---|---|
| 편집 감각 | 숨김 마커 경계에서 캐럿·선택·삭제가 예측대로 움직인다. 파싱 결과가 적용될 때 깜빡임이나 스크롤 이동이 없다. 이미지·수식이 나타나도 읽던 위치가 유지된다. | 캐럿 규칙은 테스트됨. 스크롤 보정은 `scroll(to: origin)` 휴리스틱에 의존. 창 크기 변경 시 모든 렌더가 사라졌다 다시 나타남. | W2, W8 |
| 파일이 곧 문서 | 바이트 단위 보존. 외부 도구의 이동·삭제·덮어쓰기에 데이터 손실 없이 반응. 저장 실패가 완료로 보이지 않는다. | 핵심 경로는 테스트됨. OS가 전달하는 조정 알림·디스크 부족·권한 상실은 미검증. | W5 |
| 크기와 무관한 반응 | 100KB·1MB·10MB에서 입력 지연이 사람이 느끼지 못하는 범위에 있다. | 1MB에서 편집기 단독 p95 9.05ms(예산 4ms 초과). 10MB 편집기 미측정. | W1, W3, W6 |
| 실패의 국소성 | 렌더 실패·시간 초과·WebContent 종료는 해당 요소만 원문으로 되돌린다. 나머지 렌더와 입력은 계속된다. | 스냅샷 대기에 상한이 없고, 한 작업이 멈추면 뒤따르는 모든 작업이 멈출 수 있다(§2 항목 6). | W4 |
| 자원 상한 | 문서를 끝까지 훑어도 메모리가 일정 수준에서 멈춘다. | RenderService는 48MiB 상한이 있지만 편집기가 방문한 모든 요소의 이미지를 따로 붙잡는다. | W2 |
| 접근성 | 렌더 요소마다 설명이 있고, VoiceOver로 문서 구조를 따라갈 수 있다. | 라벨은 있음. VoiceOver 실기 검증 없음. | W5 |
| 코드 구조 | 규칙이 순수 타입에 있어 AppKit 없이 차등 검사할 수 있다. 한 타입이 한 책임을 가진다. | `EditorController` 618줄이 표시·캐럿·렌더 스케줄·무효화·체크박스를 모두 가진다. | W7 |
| 기본기 | 코드 강조, 표 정렬, 목록 번호, 자동 짝 등 이 부류의 편집기가 당연히 하는 것. | 대부분 없음. 이 계획의 완료 조건에는 넣지 않고 §6에서 결정한다. | W9 |

## 2. 코드에서 확인한 문제

REVIEW.md의 잔여 위험 여섯 가지에 더해, 이번 정독에서 아래를 확인했다. 재현 전이므로 "관찰"이 아니라 "코드 근거"다.

| # | 위치 | 내용 | 영향 |
|---|---|---|---|
| 1 | [EditorController.swift:196](Sources/AirMarkEditor/EditorController.swift#L196), [PresentationEdit.swift:27](Sources/AirMarkCore/PresentationEdit.swift#L27) | 입력마다 `rebased(for:)`가 스타일·요소·체크박스 배열 전체를 새로 만든다. `StyleRun.markers`가 힙 배열이므로 46k개의 retain/release가 따라온다. `presentation` didSet이 `prefixEnd`를 다시 만든다. | REVIEW 위험 1의 실체 |
| 2 | [EditorController.swift:185](Sources/AirMarkEditor/EditorController.swift#L185) | 파싱 적용 시 `Set(old.styles).symmetricDifference(Set(result.styles))`가 메인 스레드에서 스타일 92k개를 해시한다. | 파싱 적용 지연, 1MB에서 측정 필요 |
| 3 | [EditorController.swift:135](Sources/AirMarkEditor/EditorController.swift#L135) | 입력마다 `errors.removeAll()`. 화면 안에 잘못된 수식이 하나 있으면 문서 어디를 편집해도 파싱 뒤 그 수식을 WebKit에 다시 보낸다. | 입력 중 불필요한 렌더, 실패 요소의 깜빡임 |
| 4 | [EditorController.swift:222](Sources/AirMarkEditor/EditorController.swift#L222) | 렌더 환경(너비 포함)이 바뀌면 모든 artifact를 버리고 요소 전체를 무효화한다. 창을 끌어 크기를 바꾸는 동안 매 프레임 발생한다. | 크기 조절 중 렌더가 사라졌다 다시 나타남 |
| 5 | [EditorController.swift:278](Sources/AirMarkEditor/EditorController.swift#L278) | `styles(intersecting:)`는 첫 후보 이후를 선형으로 걷는다. 수천 문단에 걸친 인용 하나가 있으면 그 안의 모든 문단 조회와 캐럿 이동이 인용 전체의 스타일을 훑는다. `concealUnits(near:)`도 같은 경로를 쓴다. | REVIEW 위험 3의 컨테이너 부분 |
| 6 | [RenderService.swift:144](Sources/AirMarkRender/RenderService.swift#L144), [RenderService.swift:201](Sources/AirMarkRender/RenderService.swift#L201), [RenderService.swift:232](Sources/AirMarkRender/RenderService.swift#L232) | `takeSnapshot`에 시간 상한이 없다. WebContent가 종료되면 델리게이트가 `web`을 nil로 만들지만 진행 중인 작업은 죽은 뷰의 스냅샷을 계속 기다린다. 작업은 `tail` 체인으로 이어지므로 하나가 멈추면 이후 모든 수식·다이어그램 렌더가 멈춘다. 호출자가 취소해도 내부 Task는 취소되지 않는다. | REVIEW 위험 4의 실체. 앱 재시작 전까지 렌더가 멈출 수 있는 경로 |
| 7 | [RenderService.swift:157](Sources/AirMarkRender/RenderService.swift#L157) | WebView를 요청한 문서의 뷰 계층으로 옮겨 붙인다. 창이 닫힌 뒤 요청이 들어오면 창 없는 뷰에서 스냅샷을 시도한다. | 여러 문서·창 종료 시나리오 미검증 |
| 8 | [MarkdownParser.swift:204](Sources/AirMarkCore/MarkdownParser.swift#L204) | 닫히지 않은 `$`마다 줄 끝까지, 닫히지 않은 `$$`마다 문서 끝까지 다시 훑는다. 같은 줄에 `$`가 n개면 줄 길이의 n배, `$$`가 m개면 문서 길이의 m배다. | REVIEW 위험 3의 수식 부분 |
| 9 | [MarkdownParser.swift:104](Sources/AirMarkCore/MarkdownParser.swift#L104), [MarkdownParser.swift:113](Sources/AirMarkCore/MarkdownParser.swift#L113), [MarkdownTextView.swift:41](Sources/AirMarkEditor/MarkdownTextView.swift#L41) | 정규식을 노드마다, Enter마다 다시 컴파일한다. | 상수 비용이지만 목록 항목 수에 비례 |
| 10 | [MarkdownTextView.swift:39](Sources/AirMarkEditor/MarkdownTextView.swift#L39), [MarkdownTextView.swift:51](Sources/AirMarkEditor/MarkdownTextView.swift#L51) | Enter마다 `string`을 브리징하고 `contains("\r\n")`으로 문서 전체를 훑는다. | 10MB에서 Enter가 느려질 경로 |
| 11 | [MarkdownDocument.swift:153](Sources/AirMarkEditor/MarkdownDocument.swift#L153) | 600ms 유휴마다 원문 전체를 JSON으로 쓴다. 선택만 바뀌어도 같다. | 10MB 문서에서 캐럿 이동마다 10MB 이상을 이스케이프해 쓰는 경로 |
| 12 | [RenderService.swift:103](Sources/AirMarkRender/RenderService.swift#L103) | 표는 메인 스레드에서 동기로 그린다. 정렬 행·셀 안 서식을 무시하고 색을 고정값으로 쓴다. | 큰 표에서 입력 정지, 표 품질 |
| 13 | [EditorController.swift:353](Sources/AirMarkEditor/EditorController.swift#L353) | 무효화 뒤 스크롤을 원래 원점으로 되돌린다. 위쪽 문단의 높이가 바뀌면 읽던 줄이 움직인다. | 편집 감각 |
| 14 | [EditorController.swift:366](Sources/AirMarkEditor/EditorController.swift#L366) 부근 | 줄 간격·문단 간격·들여쓰기·글꼴 배율이 마법 숫자로 흩어져 있다. | 디자인 조정과 테스트가 어렵다 |
| 15 | [MarkdownDocument.swift:31](Sources/AirMarkEditor/MarkdownDocument.swift#L31), [RenderService.swift:35](Sources/AirMarkRender/RenderService.swift#L35) | 복구 저장소·렌더 서비스가 정적 전역이다. | 테스트 격리와 다중 창 검증이 어렵다 |

## 3. 작업 묶음

각 묶음은 문제, 설계, 단계, 합격 조건, 검증 방법을 가진다. 합격 조건의 수치는 이 호스트(Mac17,3, 24GiB, macOS 27.0, Release)에서 먼저 확인하고, PLAN.md의 기준 머신(M1 8GB, macOS 26)에서는 별도로 측정한다. 어느 쪽도 측정 전에는 달성으로 기록하지 않는다.

### W1. 입력 경로를 편집 위치 이후로 한정한다 (REVIEW 위험 1)

**문제.** §2의 1·2. 문서 끝 입력에서도 1MB p95가 9ms를 넘는다. 원인이 배열 재생성인지, 인덱스 재구성인지, 문단 재생성인지 아직 분리되지 않았다.

**설계.** `AirMarkCore`에 `PresentationStore`를 둔다. 스타일을 구조체 배열이 아니라 배열의 구조체로 저장한다. `starts`, `ends`, `kinds`, 그리고 마커를 하나의 평면 배열과 오프셋 배열로 둔다. `apply(edit)`는 `prefixEnd`로 편집 위치 이전에 끝나는 마지막 스타일을 이진 탐색해 pivot을 얻고, pivot 이후만 정수 연산으로 이동·확장한다. `prefixEnd`도 pivot 이후만 다시 계산한다. 할당이 없고 비용은 pivot 이후 개수에 비례한다. 문서 앞 입력이 여전히 문제가 되면 "pivot 이후 미적용 delta"를 지연 적용하는 두 번째 단계를 둔다. 그 결정은 측정 뒤에 한다. 파싱 적용의 차이 계산은 시작 위치로 정렬된 두 배열을 병합 순회로 비교해 해시를 없앤다. `artifacts`·`errors`·`renderTokens`는 편집 위치 이후 항목만 키를 옮긴다.

**단계.**
1. Release 빌드에 signpost 구간 `Rebase`, `StyleIndex`, `ApplyParse`, `Paragraph`를 추가하고 1MB·10MB에서 Time Profiler와 Allocations로 입력 한 번의 비용을 분해한다. 결과를 `Validation/`에 남긴다.
2. `PresentationStore`를 구현한다. 기존 `ParsedDocument.rebased(for:)`는 테스트 타깃으로 옮겨 기준 구현으로 남긴다.
3. 임의 UTF-16 편집 1,000회(결정적 난수, 삽입·삭제·치환, surrogate 경계 포함)에 대해 새 저장소와 기준 구현의 결과가 같음을 확인한다.
4. `applyParsedDocument`의 차이 계산을 병합 순회로 바꾼다.
5. `ScaleTests`에 10MB 시나리오를 추가한다. 기본 실행 시간을 지키기 위해 환경 변수로 켠다.

**합격 조건.**
- Release `ScaleTests` 편집기 단독 1MB: p95 4ms 이하, max 8ms 이하. 독립 실행 3회 모두.
- NSDocument 포함 10MB head/middle/tail: p50 8ms 이하(제안값, 측정 뒤 조정).
- 입력 한 번에 문서 크기에 비례하는 배열 할당이 없다(Allocations로 확인).
- 차등 검사 통과. 기존 43개 테스트와 UI 5개 통과.

### W2. artifact 보유량에 상한을 두고 높이를 유지한다 (REVIEW 위험 2)

**문제.** `EditorController.artifacts`가 방문한 모든 요소의 `CGImage`를 붙잡는다. RenderService가 퇴출한 이미지도 편집기가 붙잡고 있으면 해제되지 않는다. 단순히 버리면 문단이 원문 높이로 돌아가 스크롤이 튄다.

**설계.** `ArtifactStore`를 둔다. viewport ± 여유 범위 안의 요소는 픽셀을 유지하고, 그 밖은 픽셀을 버리되 `ArtifactMetrics {size, baseline, label}`는 남긴다. 문단 생성기는 metrics만 있는 요소에 같은 크기의 빈 attachment를 넣어 높이를 유지한다. 다시 화면에 들어오면 RenderService 캐시(대부분 적중)나 재렌더로 픽셀을 채우고, 높이가 같으므로 레이아웃은 움직이지 않는다. 너비가 바뀌면 metrics는 새 artifact가 도착할 때까지 배율 조정으로 임시 사용한다(display 수식은 이미 `factor`로 축소한다). 바이트 상한(제안 32MiB)과 거리 상한을 함께 둔다.

**단계.**
1. 서로 다른 수식·다이어그램·이미지 2,000개를 가진 픽스처를 만든다. 내용이 모두 달라 캐시가 병합하지 못하게 한다.
2. `AIRMARK_MEMORY_LOG` 환경 변수가 있으면 앱이 자신의 `phys_footprint`를 주기적으로 기록하게 한다. WebContent 프로세스는 스크립트에서 책임 프로세스(responsible pid)로 AirMark에 귀속해 합산한다. 귀속 방법이 확인되지 않으면 유휴 머신에서 WebContent 전체 합계의 증분으로 대체하고 그 한계를 기록한다.
3. XCUITest로 문서를 위→아래→위로 훑는 `Scripts/memory.sh`를 만들고 변경 전 기준선을 남긴다.
4. `ArtifactStore`를 구현하고 문단 생성기가 metrics 전용 상태를 처리하게 한다.
5. 퇴출 → 재진입 전후로 해당 문단의 `layoutFragmentFrame`, 클릭 진입 좌표(`enterElement`), 스크롤 원점이 같은지 검사한다.

**합격 조건.**
- 2,000개 픽스처를 훑은 뒤 앱 footprint 증분이 상한 예산 안에 있고, 재훑기에서 더 늘지 않는다.
- 퇴출된 요소의 문단 높이와 클릭 진입 위치가 변하지 않는다.
- 같은 요소의 재렌더 횟수가 재진입 횟수를 넘지 않는다.

### W3. 긴 컨테이너와 비정상 수식 입력의 비용을 고정한다 (REVIEW 위험 3)

**문제.** §2의 5·8.

**설계.** 스타일을 두 부류로 나눈다. 컨테이너(인용, 코드 블록, 목록 항목, 규칙)는 중첩 깊이가 작고 개수가 적으므로 별도 배열에 두고 문단 조회 시 해당 문단을 포함하는 것만 걷는다. 인라인 스타일은 짧아 `prefixEnd` 이진 탐색 뒤 후보 창이 작다. 이렇게 하면 문단 조회가 컨테이너 크기와 무관해진다. 두 배열로 부족하다는 측정이 나오면 그때 max-end 세그먼트 트리를 검토한다. 수식 스캐너는 한 번의 좌→우 통과로 바꾼다. 인라인은 줄 단위로 `$` 위치를 먼저 모으고, display는 닫는 `$$`가 없다고 판정한 위치 이후를 다시 훑지 않는다. 의미는 바꾸지 않는다.

**단계.**
1. `AirMarkBench --adversarial`을 추가한다. 코퍼스: 20k줄 인용 하나(1MB), 5단계 중첩 인용·목록, 한 줄에 `$` 10k개, 닫히지 않은 `$$` 10k개, 통화 표기 `$1 $2` 100k회, `\$` 이스케이프, 20k 항목 목록에 인라인 강조. 변경 전 결과를 `Validation/`에 고정한다.
2. 문단 조회가 검사한 스타일 개수를 테스트 훅으로 노출해 컨테이너 크기와 무관함을 검사한다.
3. 스캐너를 다시 쓰고, 기존 스캐너를 테스트 기준 구현으로 남겨 코퍼스와 무작위 입력(결정적 난수 10k건)에서 결과가 같음을 확인한다.

**합격 조건.**
- 각 adversarial 코퍼스의 파싱 p50이 같은 크기 일반 문서의 2배 이내.
- 인용 내부 문단 조회의 검사 개수가 인용 길이와 무관하다.
- 스캐너 차등 검사 통과.

### W4. WebKit 작업을 상태 기계로 만들고 실패를 국소화한다 (REVIEW 위험 4)

**문제.** §2의 6·7. 특히 스냅샷 대기 무한과 `tail` 체인 정체는 앱을 다시 시작하기 전까지 모든 렌더가 멈추는 경로다.

**설계.** `RenderJob {id, element, environment, generation}`을 단위로 한다. `WebRenderer`는 `idle / loading(generation) / ready(generation, view) / busy(job)` 상태를 가진다. 로드·JavaScript·스냅샷의 모든 대기는 하나의 `withTimeout` 도우미를 거친다. WebContent 종료는 generation을 올리고 진행 중 작업을 `processTerminated`로 즉시 실패시킨다. 이 실패는 한 번 재시도한다. 작업 대기열은 `tail` 체인 대신 명시적 FIFO로 두어 실패나 취소가 뒤를 막지 않게 한다. 호출자 취소는 `withTaskCancellationHandler`로 작업에 전달한다. JavaScript는 중단할 수 없으므로 결과를 버리고, 예산을 넘긴 뷰는 폐기한다. 호스트는 "현재 key 문서 창"으로 명시하고 창이 없으면 대기한다. 편집기 쪽 `errors`는 입력마다 지우지 않고 편집 위치에 따라 옮긴다. 시간 초과·종료는 1초 뒤 한 번 재시도하고, 잘못된 TeX는 내용이 바뀔 때까지 유지한다.

**단계.**
1. 재현 시험을 먼저 만든다. (a) 느린 Mermaid(노드 400개) 렌더 중 `webViewWebContentProcessDidTerminate`를 직접 호출. (b) 테스트 프로세스가 만든 WebContent를 `kill -9`로 종료. 테스트 프로세스가 유일한 WebKit 클라이언트임을 pid 집합 비교로 확인한다. (c) 시간 초과 직후 같은 요소 재요청. (d) 두 문서를 열고 하나를 렌더 중 닫기. (e) 최소화 중 요청 보류, 복원 후 재개. (f) 동시 요청 40개.
2. 변경 전 각 시험의 실제 결과를 기록한다. 정체가 재현되면 그것이 수정의 근거다.
3. 상태 기계와 대기열을 구현한다.
4. `errors` 재배치와 재시도 정책을 편집기에 넣는다.

**합격 조건.**
- 어떤 작업도 상한 시간을 넘겨 기다리지 않는다. 한 작업의 실패가 다음 작업의 시작을 막지 않는다.
- 여섯 시나리오가 `RenderTests`·`EditorTests`로 자동화되어 통과한다.
- 무관한 위치의 입력이 실패 요소의 재렌더를 일으키지 않는다.

### W5. 파일 시스템과 제품을 실제 경로로 검증한다 (REVIEW 위험 5)

**설계.** presenter 훅을 직접 부르는 대신 테스트 프로세스가 `NSFileCoordinator`로 쓰기·이동·삭제를 수행한다. 같은 프로세스 안의 다른 presenter에도 조정 알림이 전달되므로 OS 경로를 거친 검사가 된다. 디스크 부족은 `hdiutil attach -nomount ram://`로 만든 작은 RAM 디스크에 저장해 재현한다. 권한 상실은 파일과 디렉터리의 권한을 바꿔 재현한다.

**단계.**
1. `DocumentTests`의 외부 변경·이동·삭제 시험을 조정 API 경로로 한 벌 더 만든다. 결과가 직접 호출과 같아야 한다.
2. 디스크 부족·권한 상실 시험. 저장이 실패하고, 기준 바이트와 dirty 상태가 유지되고, 원본 파일이 손상되지 않아야 한다.
3. 충돌 뒤 Save As의 실제 창 흐름(이미지 기준 경로 갱신, 복구 기록 갱신)을 UI 테스트로 만든다.
4. 한글 입력기 수동 점검표를 문서화한다. 조합 중 굵게 마커 경계, 조합 중 Enter의 목록 이어쓰기, 후보창이 있는 입력기(일본어), 조합 중 파싱 적용 억제와 커밋 후 반영. 결과는 DEV_LOG에 기록한다.
5. VoiceOver 점검표. 헤딩·링크·표·수식·이미지를 순서대로 읽고, 숨김 마커가 어떻게 읽히는지 기록한다. 개선이 필요하면 별도 작업으로 만든다.
6. macOS 26 검증 환경. 이 호스트에서 Virtualization.framework 기반 VM(예: `tart`)에 macOS 26을 올려 Swift 테스트·UI 테스트·`measure.sh`를 실행한다. 실기가 확보되면 같은 절차를 반복한다.
7. 10MB 편집 UI. `measure.sh`에 10MB 문서 실행을 추가하고 입력·스크롤·저장·종료를 UI 테스트로 확인한다.

**합격 조건.** 조정 경로 시험이 직접 호출 시험과 같은 결과를 낸다. 실패 저장이 데이터를 잃지 않는다. macOS 26 VM에서 전체 테스트가 통과하고, 실패하면 그 항목이 기록된다.

### W6. 사용자가 보는 지연을 측정한다 (REVIEW 위험 6)

**설계.** 메인 스레드 호출 시간과 화면 표시 시간을 구분한다. `keyDown → didChangeText`(Keystroke), `파싱 적용 → 무효화 적용`(Reveal), `무효화 적용 → 표시 커밋`(Commit)에 signpost 구간을 둔다. `xctrace record`를 Time Profiler와 Animation Hitches 템플릿으로 감싼 `Scripts/profile.sh`를 만든다. XCUITest가 200자를 입력하고 문서를 훑는 동안 기록하고, `xctrace export`로 구간 분포와 hitch 비율을 뽑아 `Validation/`에 남긴다. 카메라 없이 입력→광자 지연은 측정할 수 없으므로 그렇게 기록한다. cold launch는 재부팅 직후 첫 실행을 기준 머신에서 수동 절차로 잰다.

**합격 조건.** 도구가 재현 가능하고 결과가 저장된다. PLAN.md의 60Hz 입력→표시 p95 33ms, 입력 종료 후 서식 반영 p95 100ms는 이 도구로 측정한 뒤에만 통과 여부를 말한다.

### W7. 구조를 나눈다

**설계.** 규칙은 `AirMarkCore`의 순수 타입으로, AppKit 결합은 얇게 남긴다.

| 새 타입 | 위치 | 가져오는 책임 | 현재 위치 |
|---|---|---|---|
| `PresentationStore` | Core | 스타일·요소·체크박스 저장, 편집 재배치, 구간 조회 | `presentation`, `prefixEnd`, `rebased`, `styles(intersecting:)` |
| `CaretPolicy` | Core | 숨김 단위 계산, 캐럿 정규화, 마커 경계 삭제 규칙 | `concealUnits`, `normalizedCaret`, `deleteBackwardAcrossMarkers` |
| `MathScanner` | Core | `$`·`$$` 구간 탐색 | `mathSpans` |
| `EditorTheme` | Editor | 글꼴 배율, 줄 간격, 들여쓰기, 색 | 문단 생성기의 마법 숫자 |
| `ParagraphStyler` | Editor | `NSTextParagraph` 생성 | `textContentStorage(_:textParagraphWith:)`, `conceal`, `taskSymbol` |
| `RenderScheduler` + `ArtifactStore` | Editor | 후보 선정, 작업·토큰, artifact 보유 | `scheduleRenders`, `artifacts`, `errors`, `renderTokens` |
| `InvalidationQueue` | Editor | 보류 무효화와 viewport 적용, 스크롤 고정 | `pendingInvalidation`, `applyPendingInvalidation` |
| `WebRenderer`(상태 기계), `TableRenderer`, `ImageLoader` | Render | 각각의 렌더 경로 | `RenderService` 내부 |

`RenderService`와 `RecoveryStore`는 정적 전역을 없애고 생성 시 주입한다. 각 추출 커밋은 동작을 바꾸지 않으며 기존 테스트 전체가 전후로 통과해야 한다. 추출된 순수 타입은 AppKit 없는 Swift Testing으로 검사한다. 한 줄에 여러 문장을 `;`로 잇는 형식은 한 문장 한 줄로 되돌리고, `.swiftformat` 설정을 커밋해 스크립트로 검사한다.

### W8. 위생 목록

§2에서 W1~W4에 속하지 않는 항목이다. 각각 작은 커밋 하나로 처리한다.

- 정규식 정적 컴파일(§2-9).
- Enter 경로의 `string` 브리징과 줄바꿈 종류 탐색 제거. 줄바꿈 종류는 문서 읽기 시 한 번 판정해 보관한다(§2-10).
- 복구 기록: 원문이 바뀌었을 때만 본문을 쓰고, 선택·스크롤만 바뀌면 작은 sidecar를 쓰거나 5초 이상으로 합친다. 10MB에서 기록 비용을 측정한다(§2-11).
- 창 크기 변경: 라이브 리사이즈 종료 알림 뒤 150ms에 한 번만 환경을 갱신하고, 그 사이 기존 artifact를 배율로 유지한다(§2-4).
- 스크롤 고정: 무효화 전 첫 가시 문단의 위치와 y를 기억하고, 적용 뒤 그 문단이 같은 y에 오도록 맞춘다(§2-13).
- 표: 정렬 행과 셀 안 인라인 서식을 반영하고, CoreText로 오프스크린에서 그려 메인 스레드를 비운다. 색은 환경의 외관으로 해석한다(§2-12).
- `RenderService.touch`의 선형 LRU는 W2로 항목 수가 늘면 순서 유지 사전으로 바꾼다.
- `onChange`마다 원문 전체를 스냅샷에 복사하는 비용을 10MB에서 측정하고, 필요하면 저장·복구 시점에만 revision 기준으로 읽는다.

### W9. 제품 기본기 후보 (결정 필요)

이 계획의 완료 조건에 넣지 않는다. M3 이후 우선순위를 정한다. 후보: 펜스 코드 문법 강조(네이티브 토크나이저 또는 tree-sitter), 표 열 정렬·셀 서식·Tab 이동, 순서 목록 번호 재계산, 괄호·따옴표 자동 짝, 이미지 붙여넣기(PLAN.md에서 보류), 목차 사이드바, 집중·타자기 모드, PDF 내보내기. 각각 원문 보존과 undo 경로에 미치는 영향을 먼저 적은 뒤 시작한다.

## 4. 순서와 관문

| 단계 | 내용 | 완료 조건 | 산출물 |
|---|---|---|---|
| M1 측정 기반 | W6 도구, W1-1 프로파일, W3-1 코퍼스 기준선, W2-1~3 메모리 기준선, W4-1~2 재현 시험 | 모든 최적화 대상에 변경 전 수치와 재현 절차가 있다 | `Validation/2026-09-xx-baseline/`, DEV_LOG 항목 |
| M2 코어 정리와 입력 경로 | W7의 Core 추출, W1, W3 | W1·W3 합격 조건. 차등 검사 통과 | Core 테스트 확대, 벤치 갱신 |
| M3 렌더 수명주기와 메모리 | W4, W2, W7의 Render·Editor 추출 | W4·W2 합격 조건 | 시나리오 테스트, 메모리 측정 |
| M4 파일과 제품 검증 | W5, W8 | 조정 경로·실패 저장 시험 통과, macOS 26 VM 결과 기록, 수동 점검표 결과 기록 | DEV_LOG, 점검표 |
| M5 마감 | 남은 W8, 문서 정리, 기준 머신 측정, W9 결정 | README·PLAN·DEV_LOG가 현재 상태를 정확히 말한다 | 최종 측정 기록 |

M1을 건너뛰고 M2로 가지 않는다. 변경 전 수치가 없는 최적화는 성과를 말할 수 없다. M2와 M3은 의존이 없어 병렬로 진행할 수 있지만, 두 작업이 모두 `EditorController`를 나누므로 W7의 추출 커밋을 먼저 합친 뒤 갈라진다.

## 5. 측정 규약

- Release 빌드. 호스트 모델, 메모리, OS·Xcode·Swift 버전을 매 기록에 적는다.
- 백분위는 nearest-rank(`ceil(p·n)`). tail 주장에는 독립 실행 3회 이상.
- 첫 실행을 cold로 부르지 않는다. OS 캐시 상태를 통제하지 않았으면 그렇게 적는다.
- 변경 전·후는 같은 하니스, 같은 픽스처, 같은 반복 수로 잰다. 원시 출력은 `Validation/<날짜>-<주제>/`에 두고 README에 재현 명령을 적는다.
- 기준 머신(M1 8GB, macOS 26)에서 잰 값만 PLAN.md 예산의 통과 근거가 된다. 이 호스트의 값은 방향 확인이다.
- 각 단계가 끝나면 DEV_LOG.md에 실제 검증과 한계를 적는다. 하지 않은 것을 한 것처럼 쓰지 않는다.

## 6. 결정이 필요한 사항

1. macOS 26 검증을 VM으로 시작해도 되는가. 실기 확보 계획이 있는가.
2. W9 후보 중 이번 주기에 넣을 것. 코드 강조와 표 편집은 편집기 부류의 기본기이지만 각각 며칠 규모다.
3. 10MB 입력 예산. PLAN.md는 4ms를 100KB 기준으로 두었다. 10MB의 목표를 8ms로 두는 제안이 타당한지.
4. 렌더 실패 재시도 정책(1초 뒤 1회)과 artifact 상한(32MiB)의 초기값.

## 7. 하지 않을 것

- 자체 Markdown 파서를 만들지 않는다. 의미 해석은 swift-markdown에 남긴다.
- TextKit 1로 내려가지 않는다. `NSTextTable`은 TextKit 1 전용이므로 표는 attachment 경로를 유지한다.
- SwiftMath 도입은 이번 주기에 검토하지 않는다.
- 원격 이미지, 폴더 작업 공간, 동기화는 범위 밖이다.
